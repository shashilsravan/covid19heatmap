import React from 'react'

export default function({text}) {
    return (
        <div>
            <h2 className='about-title'>
                <span>{text}</span>
            </h2>
        </div>
    )
}
